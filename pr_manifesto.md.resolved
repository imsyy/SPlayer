# Pull Request 汇总 (自 Fork 以来的主要变更)

这是一份自你 Fork 本项目以来所做的所有功能改进、Bug 修复和架构优化的完整汇总，你可以直接用于 PR 描述。

---

## 🚀 核心功能增强 (Features)

### 1. 本地歌曲匹配增强 (Local Song Matching)
- **接入 `/search/match` API**：显著提升了本地音乐与网易云元数据的匹配准确率。
- **自定义匹配等级**：引入了 `Strict` (严格)、`Normal` (标准)、`Loose` (宽松) 三种策略，用户可根据需求调整匹配灵敏度。
- **匹配缓存优化**：优化了匹配结果的本地持久化逻辑，减少重复请求。

### 2. 歌词系统重大升级 (Lyric System)
- **TTML 支持优化**：
  - 完美支持 TTML 逐字歌词解析。
  - **翻译语言智能优选**：实现了在多语言 TTML 中优先选择简体中文翻译的逻辑。
- **“拒绝胎教” Mode**：新增功能，可一键屏蔽歌词中的拼音音译（罗马音），保持界面简洁。
- **本地歌词优先级修复**：重构了歌词搜索逻辑，确保与歌曲同路径的 `.lrc` 或 `.ttml` 文件具有最高优先级。
- **歌词同步与对齐**：优化了逐字歌词（YRC/TTML）的对齐算法，修复了部分翻译错位问题。

### 3. 下载与存储管理 (Download & Storage)
- **缓存重构**：优化了歌词和歌曲元数据的缓存管理。
- **下载逻辑优化**：改进了下载队列的稳定性。

---

## 🛠️ 播放体验与稳定性 (Stability & Fixes)

### 1. 播放逻辑修复
- **异常切歌修复**：解决了在某些网络环境或文件格式下，歌曲播放中途异常跳转下一首的问题。
- **音频缓冲优化**：改进了 Rust 后端的音频流处理，提升了播放流畅度。

### 2. 移动端 (Android) 支持
- **Media Session 集成**：完善了 Android 端的媒体通知栏控制（播放/暂停/切歌）。
- **Release 签名方案**：优化了 Android 包的签名配置，支持通过环境变量自动签名，提升了 CI 构建成功率。

### 3. UI 与交互 (UI/UX)
- **搜索体验优化**：修复了搜索结果显示不全及部分接口报错。
- **歌词显示修复**：修复了缩放窗口时歌词闪烁或丢失的问题。

---

## 👷 构建与架构 (CI/CD & Architecture)

- **GitHub Actions 优化**：
  - 对 `dev` 分支进行了构建瘦身，目前仅针对 Windows 平台生成 Artifact，大幅节省了 Action 运行资源和时间。
  - 修复了构建流程中 Artifact 上传插件的版本兼容性问题。
- **TypeScript 类型补完**：持续推进项目的类型安全，将多个核心服务和 API 路由从 JS 迁移至 TS。

---

## 📝 总结建议

这个 PR 涵盖了从底层播放引擎稳定性到上层歌词交互体验的全方位优化。建议 PR 标题定为：
**`feat: 增强本地歌词匹配、新增 TTML 多语言处理能力及播放稳定性优化`**
