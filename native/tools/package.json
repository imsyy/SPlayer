{
  "name": "tools",
  "version": "0.1.0",
  "main": "tools.node",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "tools"
  },
  "scripts": {
    "build": "napi build --release --no-const-enum",
    "build:debug": "napi build --no-const-enum"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3"
  }
}
